#ifndef ARRAY_H_INCLUDED
#define ARRAY_H_INCLUDED

int* create_array(int length);
int* init_array(int* arr, int length);
void print_array(int* arr, int length);
int get_max_arr(int* arr, int length);

#endif // ARRAY_H_INCLUDED
